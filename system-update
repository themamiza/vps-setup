#!/bin/sh

printf "# Updating the system
#> apt update && apt upgrade\n"

apt update && apt upgrade

printf "# Installing programs\n"
progs="tmux python ufw htop"
for p in $progs; do
    printf "#> apt install %s\n" "$p"
    apt install "$p" -y
done
